server:
  port: 8082
spring:
  application:
    name: buy

  datasource:
    username: pg3402
    url: jdbc:postgresql://localhost:5432/user
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true